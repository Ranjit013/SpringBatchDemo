drop table PEOPLE;
CREATE TABLE PEOPLE (
                        person_id int NOT NULL,
                        first_name varchar(255) NOT NULL,
                        last_name varchar(255),
                        PRIMARY KEY (person_id)
);
